#include "Algorithm.hpp"

std::vector<unsigned char> Convolution::operator()(std::vector<unsigned char>& imageData, size_t width, size_t height, Filter& filter) {

    // 创建一个新的图像数据用于存储卷积结果
    std::vector<unsigned char> result(imageData.size(), 0);
    return result;
}